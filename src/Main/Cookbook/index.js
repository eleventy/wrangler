import React from 'react'

const Cookbook = () => {
  return (
    <div style={styles.root}>
      Cookbook
    </div>
  )
}
export default Cookbook

/// //////////

const styles = {
  root: {
    display: 'flex',
    flexDirection: 'column',
    height: '100vh',
    paddingTop: 50
  }
}
